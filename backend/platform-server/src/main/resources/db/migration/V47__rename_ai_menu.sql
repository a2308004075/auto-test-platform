-- =====================================================================
-- V47: AI 目录菜单更名为 AI管理
-- =====================================================================

-- AI 目录菜单：【AI】更名为【AI管理】（id=125，权限编码不变，角色分配不受影响）
UPDATE `sys_menu` SET `name` = 'AI管理', `updated_at` = NOW() WHERE `id` = 125;
